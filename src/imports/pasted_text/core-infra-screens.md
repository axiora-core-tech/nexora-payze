LAYER 1 — CORE INFRASTRUCTURE SCREENS

[SCREEN: API Key Management]
Build a developer-facing API key management screen. Display a list of all active API keys per client with columns: key name, key prefix (masked), environment (Live / Test), creation date, last used date, and status (Active / Revoked). Include a button to generate a new API key that opens a modal asking for key name, environment selection, and optional IP whitelist entries. Show a one-time reveal of the full key on creation with a copy button and a warning that the key will not be shown again. Include a revoke action per key with a confirmation modal. Add a webhook endpoints section below the key list where users can add endpoint URLs, select which events to subscribe to (payment.created, payment.captured, payment.failed, refund.initiated, refund.completed, dispute.opened, mandate.created, mandate.paused, mandate.cancelled, settlement.processed), and view delivery logs per endpoint with status codes and retry history. Include a signature verification section showing the webhook secret and instructions for HMAC validation.

[SCREEN: Sandbox / Test Environment Toggle]
Design a persistent environment switcher visible in the top navigation bar of every screen. Show a clearly labelled toggle between Live and Test modes. When in Test mode, display a full-width banner at the top of every screen indicating the user is in sandbox mode. In Test mode, include a test data panel accessible from the sidebar with pre-configured test card numbers for success, decline, 3DS challenge, and timeout scenarios. Include test UPI VPAs, test bank accounts for NACH simulation, and test wallet credentials. Provide a "reset test data" action that clears all sandbox transactions and resets counters. Show a test event simulator where developers can manually trigger webhook events by selecting event type, payload template, and target endpoint.

[SCREEN: Fraud & Risk Configuration]
Build a risk rules configuration screen accessible to super admins and client admins. Show a rules list with each rule containing: rule name, trigger condition (velocity, amount threshold, geography, device fingerprint mismatch, BIN mismatch), action (allow, review, block), and toggle to enable or disable. Include a form to create custom rules using a condition builder: select field (IP country, card BIN, transaction amount, transaction frequency in last N minutes, same card on multiple accounts), operator (equals, greater than, less than, in list, not in list), and value. Allow multiple conditions per rule with AND/OR logic. Include a fraud review queue below the rules panel showing transactions flagged for review with fields: transaction ID, amount, method, risk score, triggered rule name, and actions (approve, decline, escalate). Show a risk score distribution chart showing volume across score bands (0–30 low, 31–60 medium, 61–100 high).

[SCREEN: Settlement Configuration]
Design a settlement configuration screen. Show a settlement schedule section with options: T+1, T+2, T+3, weekly, and instant settlement (with a fee indicator). Allow super admins to set different schedules per client. Include a payout method section where clients add their bank accounts: account holder name, account number, IFSC code, account type (current/savings), and penny drop verification status. Allow multiple bank accounts with one marked as primary. Add a settlement log table showing each settlement batch: batch ID, settlement date, number of transactions, gross amount, fees deducted, tax on fees, net amount, payout status (pending, processing, credited, failed), and UTR number. Include a settlement reconciliation action per batch that generates a line-by-line report matching each transaction to the settlement payout.

LAYER 2 — ONBOARDING & ACCESS CONTROL

[SCREEN: Merchant Onboarding Wizard — Step 1: Business Details]
Build the first step of a multi-step merchant onboarding wizard. Show a form with: business legal name, business type selector (D2C / E-commerce, SaaS / Subscription, Freelancer / Agency, Brick-and-mortar / Offline Merchant, Marketplace, Enterprise), business PAN number with live format validation, GSTIN field with real-time verification status indicator (unverified, verifying, verified, invalid), registered address with pincode-based city and state auto-fill, business website or app store URL, average monthly transaction volume (range selector), and primary contact name, email, and mobile number. Show a persistent step progress indicator at the top showing all 6 steps. Include autosave status in the top right. Allow back and next navigation with field-level inline validation on next click.

[SCREEN: Merchant Onboarding Wizard — Step 2: KYC / KYB Documents]
Build step 2 of the onboarding wizard focused on document collection. Show upload slots for: PAN card (individual or business), GST registration certificate, cancelled cheque or bank statement, Certificate of Incorporation (for companies), director / proprietor Aadhaar (masked except last 4 digits), and board resolution (for enterprises). Each upload slot shows: document name, required or optional label, accepted formats and size limit, upload area with drag-and-drop and file browse, and a status indicator with states: not uploaded, uploading (with progress bar), pending review, verified (with green check), rejected (with rejection reason and re-upload prompt). Show a compliance checklist sidebar showing percentage of required documents completed. Include a note section where the merchant can add context for the compliance team.

[SCREEN: Merchant Onboarding Wizard — Step 3: Bank Account Verification]
Build step 3 of the onboarding wizard for bank account setup. Show a form for: bank account number (with show/hide toggle), confirm account number, IFSC code with bank name and branch auto-populated on valid IFSC entry, account holder name, and account type (current / savings). Below the form, show a penny drop verification section with a trigger button labelled "Verify account instantly". Show three states for penny drop: idle (with instructions), in-progress (with a spinner and message "Sending ₹1 to your account"), and completed (with success or failure message and the exact name returned from the bank). On failure, show a manual verification fallback option using bank statement upload. Allow up to 3 bank accounts to be added with one set as primary.

[SCREEN: Merchant Onboarding Wizard — Step 4: Payment Method Selection]
Build step 4 of the onboarding wizard for selecting enabled payment methods. Display a grid of payment method cards, each with: method icon, method name, brief description, estimated activation time, and a toggle to enable or disable. Methods to include: Credit Card (Visa, Mastercard, Amex, RuPay), Debit Card, UPI Collect, UPI Intent, UPI QR, UPI AutoPay (recurring), NACH E-Mandate, Net Banking (list of 50+ banks), Paytm Wallet, PhonePe Wallet, MobiKwik, Amazon Pay, Apple Pay, Google Pay, Samsung Pay, LazyPay (BNPL), Simpl (BNPL), ZestMoney (BNPL), EMI on Card, Cardless EMI, SEPA Direct Debit, ACH Debit, International Cards (non-India). Show a "Recommended for your business type" section at the top that highlights a pre-selected set based on the business type chosen in Step 1. Show estimated approval timelines per method. Show a combined fee structure summary at the bottom listing MDR rates per selected method.

[SCREEN: Merchant Onboarding Wizard — Step 5: White-Label Setup]
Build step 5 for brand customisation of the hosted checkout experience. Show fields for: logo upload (with preview of how it will appear on the checkout page), brand primary color picker, checkout page title, custom domain setup (with DNS CNAME instructions and verification status), email sender name (shown in "From" field of all notification emails), email sender address (with domain verification status), SMS sender ID (for branded SMS), and a default payment description prefix that appears on bank statements. Show a live checkout page preview panel on the right side that updates in real time as the merchant fills in each field, simulating how their branded checkout will appear to their end customers.

[SCREEN: Merchant Onboarding Wizard — Step 6: Compliance & Activation]
Build step 6, the final onboarding step. Show a compliance mode selection section explaining the difference between operating as a Payment Aggregator (requires RBI licence, holds customer funds) and a Software-Only Payment Gateway (uses a licensed underlying gateway, no fund aggregation). Include a decision guide with a simple 3-question flow to help the merchant understand which model applies to them. Show a terms and conditions acceptance with a scrollable terms box and checkbox. Show a data processing agreement acceptance. Show a final onboarding summary card listing all chosen settings: business name, enabled payment methods count, settlement schedule, bank account last 4 digits, and compliance mode. Include an "Activate Account" button and a "Save as Draft" option. On activation, show a confirmation screen with next steps and support contact.

[SCREEN: Super Admin Portal — Platform Overview]
Build the super admin home screen for the platform operator. Show a top metrics bar with: Total Platform GMV (with INR / USD toggle and time range selector: today, this week, this month, custom), Active Merchants, Total Transactions Today, Platform-Wide Failure Rate, and Total Settlements Disbursed This Month. Below show a merchant portfolio table with columns: merchant name, business type, activation date, GMV this month, transaction count, failure rate, dispute rate, settlement status, and compliance mode. Include row-level actions: view details, edit settings, suspend, and impersonate (view as that merchant). Show a platform health panel with real-time metrics: gateway latency, payment method uptime status (green/amber/red per method), webhook delivery success rate, and fraud block rate. Include a pending actions queue: merchants awaiting KYC approval, disputed transactions awaiting evidence, and compliance alerts.

[SCREEN: Super Admin — Merchant Detail View]
Build the drill-down screen for a single merchant viewed from the super admin portal. Show a header with merchant name, business type, GSTIN, activation date, and account status badge. Show tabs for: Overview, Transactions, Settlements, Disputes, Configuration, and Audit Log. In the Overview tab: GMV trend chart (last 30 days), payment method mix pie chart, top failure reasons bar chart, and a settlement history list. In Configuration tab: editable MDR rates per payment method, settlement schedule override, risk threshold overrides, feature flag toggles (enable or disable specific payment methods for this merchant), and monthly volume caps. In Audit Log tab: a timestamped log of every configuration change made by any admin, showing who made the change, what was changed, previous value, and new value.

[SCREEN: Client Admin Dashboard — Home]
Build the home screen for merchant client admins after login. Show a greeting with the client's business name and today's date. Show four primary metric cards: Today's Collections, This Month's Collections, Pending Settlements, and Active Payment Links. Below show a real-time transaction feed with columns: time, customer identifier (masked), amount, payment method, and status (success, failed, pending, refunded). Show a quick-actions bar with buttons: Create Payment Link, Generate Invoice, Send Payment Reminder, Add Team Member, and View Settlement Report. Show a payment method performance summary: a mini bar chart showing today's transaction count and success rate per enabled method. Show a recent disputes section at the bottom listing any open chargebacks with days remaining to respond.

[SCREEN: Client Staff Access — Restricted View]
Build the interface for a client staff member with restricted permissions. Show only the features their role permits. A cashier role should see: today's transactions, a QR code display for in-store payments, a manual payment link creation form, and a basic daily summary. A finance role should see: full transaction history, settlement reports, invoice management, and GST export. A support role should see: transaction search, refund initiation (up to a configured amount limit), dispute log, and customer payment status lookup. In all restricted views, show clearly disabled states with lock icons on sections the staff member does not have access to. Include a "Request access" link on each locked section that sends an email to the client admin.

LAYER 3 — PAYMENT METHODS & PAYMENT LINKS

[SCREEN: Checkout Page — Unified Payment UI]
Build the end-customer-facing checkout page. Show the merchant's branded header (logo, name, brand color). Show an order summary section with: amount payable, description, and merchant reference. Below the order summary show a payment method selector with grouped tabs: Cards, UPI, Net Banking, Wallets, BNPL, and EMI. In the Cards tab: card number input with live card network detection (Visa, Mastercard, Amex, RuPay icon appearing on type), expiry month and year, CVV, and cardholder name. Show a saved cards section for returning customers with masked card display and CVV re-entry. In the UPI tab: UPI ID input with a validate button, a list of UPI intent app icons (GPay, PhonePe, Paytm, BHIM), and a QR code option. In Net Banking: a searchable bank list with logos. In Wallets: wallet icons with balance display for verified returning users. In BNPL: eligibility check with a "Check eligibility" button before showing LazyPay, Amazon Pay, Simpl options. In EMI: card EMI with tenure selector and per-month amount calculator, and cardless EMI options. Show Apple Pay and Google Pay buttons conditionally based on device and browser capability. Show a total payable amount (including any convenience fee) updating dynamically as the user selects method. Show security trust badges at the bottom.

[SCREEN: Dynamic QR Code Generator]
Build a QR code management screen for offline and hybrid merchants. Show two tabs: Dynamic QR and Static QR. In Dynamic QR: a form to enter amount, description, and optional expiry time (in minutes). On generate, show the QR code in a large display optimised for screen or print, the payment link equivalent of the QR, a countdown timer if expiry is set, and real-time status (awaiting payment, payment received with amount and method, expired). Include a print-optimised layout option for dynamic QR. In Static QR: show the merchant's permanent UPI QR, download options (PNG, SVG, print-ready PDF), and a link to the merchant's UPI VPA. Below both tabs show a QR payment log: date, amount, method used, and transaction status.

[SCREEN: Payment Link Creator & Manager]
Build the payment link management screen. Show a "Create Payment Link" form with: link title or description, amount (fixed or enter-at-checkout option), currency selector, payment methods to accept (inherited from account defaults with option to restrict per link), link expiry (date and time picker or never), usage type (single-use or multi-use with optional maximum usage count), customer name and email (optional pre-fill), and a custom notes field visible to the payer. On creation, show the generated short link with copy, share via email, share via SMS, share via WhatsApp, and download as QR code options. Show a payment links table with columns: link title, amount, created date, expiry, usage (e.g. 3/10), total collected, status (active, expired, completed, paused), and actions (view audit, copy link, deactivate, duplicate). Include a link detail drill-down showing every individual transaction against that link: transaction ID, payer name (if captured), amount, method, time, and status.

[SCREEN: Payment Link Audit Trail]
Build the audit trail screen for a specific payment link. Show the link metadata at the top: title, amount, created by, created on, expiry, total expected, total collected, and balance remaining. Below show a full transaction log for this link with columns: transaction number, date and time, payer email or phone (masked), amount paid, payment method used, transaction ID, and status. Show a timeline visualisation of payments against the link showing cumulative collection over time vs the target amount. Include an export button (CSV and PDF). Show a payments summary at the bottom: total paid, number of successful payments, number of failed attempts, and number of unique payers.

[SCREEN: International Payment Methods]
Build a configuration screen for enabling international payment acceptance. Show a toggle to enable international payments. Show a currency management section where the merchant enables specific currencies they want to accept (USD, EUR, GBP, SGD, AED, AUD, CAD). Show a payment method availability matrix showing which methods are available per country/region: SEPA Direct Debit for EU, ACH Debit for US, international Visa/Mastercard/Amex for global, and local methods available in each market. Show a foreign exchange section with: live FX rate display for enabled currencies against INR, option to lock rate at checkout vs settle at time of transfer, and FX markup configuration. Show an international transaction log filtered to non-INR transactions with settled INR amount and FX rate used per transaction.

LAYER 4 — AUTO-PAY & REMINDERS

[SCREEN: Subscription & Mandate Management]
Build the recurring payment mandate management screen. Show two tabs: UPI AutoPay Mandates and NACH E-Mandates. In UPI AutoPay tab: a table of all active mandates with customer name (masked), UPI VPA, frequency (daily / weekly / monthly / quarterly / yearly), amount, start date, end date, status (pending, active, paused, cancelled, completed), next debit date, and actions (pause, cancel, view history). Show a mandate creation form: customer UPI VPA, amount, frequency, start date, end date, and description. Show per-mandate debit history with date, amount, status, and failure reason if applicable. In NACH E-Mandate tab: same layout adapted for bank account-based NACH mandates with IFSC, account number (masked), UMRN number, bank name, and registration status.

[SCREEN: Auto-Pay Reminder Engine]
Build the reminder configuration and management screen. Show a reminder templates section where clients create templates for: SMS (with character count and variable insertion: {{customer_name}}, {{amount}}, {{due_date}}, {{payment_link}}), Email (with subject line, rich text body editor, and variable insertion), and WhatsApp (plain text with variables). Show a reminder schedule builder: define the sequence of reminders before and after a due date, e.g. 7 days before, 3 days before, 1 day before (due day), 1 day after (overdue), 3 days after, 7 days after (final notice). Per schedule step allow: channel selection (SMS, email, WhatsApp, or all), template selection, and whether to stop the sequence if payment is made. Show a reminder campaign list: campaign name, linked mandate or invoice group, schedule applied, total recipients, sent count, opened count (email), clicked count, payments triggered from reminders, and status (active, paused, completed). Show a per-campaign send log with recipient, channel, sent time, delivery status, and open / click tracking.

[SCREEN: Dunning Management]
Build the dunning workflow configuration screen for failed recurring payments. Show a dunning rules panel where clients configure the retry and escalation sequence for a failed auto-pay debit. Sequence steps configurable: immediate retry (same day), retry after N days, send reminder after retry failure, escalate to email with account suspension warning, pause subscription after final failure, and trigger a webhook on dunning completion. Each step is configurable with: number of days after failure, action type (retry debit, send notification, pause subscription, cancel mandate), channel and template for notifications. Show a dunning queue table: customer name (masked), product or plan name, failed amount, failure date, failure reason (bank decline, insufficient funds, mandate expired, account closed), dunning step currently at, next action date, and manual override options (retry now, mark as resolved, write off, escalate to support). Show dunning outcome metrics: recovery rate percentage, average days to recovery, amount recovered vs written off.

[SCREEN: WhatsApp Pay Integration]
Build the WhatsApp Pay setup and management screen. Show a setup section with: WhatsApp Business Account connection (via Meta Business API credentials), phone number registration status, and WhatsApp Pay enablement toggle. Show a payment request composer: enter customer WhatsApp number, amount, description, and optional due date. On send, generate a payment request message template preview. Show a WhatsApp payment transaction log with customer number (masked), amount, status, time initiated, and time completed. Show a broadcast payment request feature: upload a CSV of customer numbers and amounts to send bulk payment requests. Show delivery and read receipt tracking per message.

LAYER 5 — ANALYTICS & REPORTING

[SCREEN: Real-Time Transaction Dashboard]
Build the primary analytics and monitoring dashboard. Show a live transaction feed updating in real time with: transaction ID, timestamp, customer masked identifier, amount, currency, payment method, gateway used, status (success / failed / pending / refunded / disputed), and time to complete (latency). Show a summary metrics strip above the feed: transactions per minute, success rate (last 5 minutes), average transaction value, and current fraud block rate. Show a payment method breakdown panel: for each enabled method show total transactions, success rate, average latency, and a mini sparkline for the last hour. Show a geographic heat map of transaction origin by Indian state and country for international. Show a time-series chart of transaction volume and success rate over the selected time window (last 1 hour, 6 hours, 24 hours, 7 days) with the ability to overlay failure rate as a second series. Include an alert configuration panel for setting thresholds: trigger notification if failure rate exceeds X% over Y minutes, if transaction volume drops below Z per minute, or if a specific payment method goes offline.

[SCREEN: Conversion Funnel Analytics]
Build the checkout conversion funnel analysis screen. Show a step-by-step funnel visualisation: checkout initiated → payment method selected → payment details entered → payment submitted → payment authorised → payment captured. Show drop-off count and percentage at each step. Allow filtering by payment method, device type (mobile / desktop / tablet), customer type (new / returning), date range, and transaction amount range. Show a method-by-method conversion comparison: which methods have the highest checkout abandonment rate, which have the highest bank decline rate, and which have the highest 3DS challenge failure rate. Show a heatmap of drop-off by hour of day and day of week. Show a top abandonment reasons analysis: timeout, bank page not loading, CVV error, OTP not received, and insufficient balance indicators. Include an A/B test results section showing performance difference between two checkout configurations if any test is running.

[SCREEN: Failure Analysis & Diagnostics]
Build the payment failure analysis screen. Show a failure breakdown by category: bank decline (further categorised into insufficient funds, do-not-honour, invalid card, expired card, restricted card), authentication failure (3DS timeout, OTP failure, incorrect OTP), technical failure (gateway timeout, network error, API error), fraud block (velocity rule, risk score, manual block), and user abandonment. Show a failure trend chart over time with category stacking. Show a top failing BINs table: card BIN prefix, issuing bank, failure rate on that BIN, transaction count, and a flag for anomalous spike. Show a failure rate by issuing bank heatmap for card transactions. Show a UPI-specific failure section: failures by error code (payment declined by bank, beneficiary account invalid, transaction limit exceeded, UPI PIN incorrect, VPA not registered). Show an action recommendations panel: if a specific bank or method failure rate exceeds 15%, show an automated recommendation with suggested action.

[SCREEN: Settlement Reconciliation Report]
Build the settlement reconciliation screen. Show a settlement batches list with: settlement date, batch reference, number of transactions included, gross transaction amount, total refunds, total disputes, platform fees, GST on fees, TDS deduction, net payout amount, bank UTR number, payout status, and bank account credited. Show a reconcile button per batch that opens a detailed reconciliation view. In the detail view: a line-by-line table matching each transaction to the settlement batch, with transaction ID, amount, fee, tax, and net contribution. Show a reconciliation summary at the top: matched transactions, unmatched transactions (with reasons: timing difference, fee discrepancy, refund mid-cycle), and total variance. Show export options: download full reconciliation as Excel or CSV with all columns. Show a discrepancy resolution workflow: flag a discrepancy, add a note, assign to a team member, and track resolution status.

[SCREEN: Cohort & LTV Analytics — Subscription Clients]
Build a cohort analysis screen relevant to SaaS and subscription merchants. Show a cohort retention table: cohorts defined by signup month on the Y-axis, months since signup on the X-axis, with cells showing percentage of cohort still paying. Colour-code cells from high retention (dark) to low retention (light). Show an average revenue per user (ARPU) trend by cohort. Show a customer lifetime value (LTV) calculator: input average monthly revenue, average churn rate, and average gross margin to compute LTV. Show a churn prediction signal section listing customers at high churn risk based on recent payment failure, declining usage, or nearing mandate end date. Show a revenue expansion chart: new MRR, expansion MRR, contraction MRR, and churned MRR for each month as a stacked bar chart.

[SCREEN: Custom Report Builder]
Build a drag-and-drop custom report creation interface. Show a dimensions panel on the left with available dimensions: date (day, week, month), payment method, currency, business type, customer geography (state, country), transaction status, settlement status, dispute status, invoice status, and mandate status. Show a metrics panel listing available metrics: transaction count, transaction volume, success rate, failure rate, average transaction value, refund count, refund amount, dispute count, dispute rate, net settled amount, platform fees, and active mandates. Allow the user to drag dimensions to rows and metrics to columns. Show a report preview updating on each change. Include filters: date range, payment method filter, merchant filter (for super admin), and status filter. Allow adding calculated metrics: custom formula using existing metrics. Show a schedule delivery section: configure daily, weekly, or monthly email delivery to specified recipients in CSV or PDF format. Show a saved reports library.

[SCREEN: Multi-Client Aggregate View — Super Admin Analytics]
Build the super admin analytics screen showing platform-wide aggregated data. Show a platform GMV chart: daily or monthly gross merchandise value across all merchants with ability to segment by business type, payment method, and geography. Show a merchant performance leaderboard: top merchants by GMV, top merchants by transaction count, and merchants with highest failure rates flagged in red. Show a payment method adoption chart: what percentage of merchants have each method enabled, and trending adoption over time. Show a platform revenue breakdown: total MDR collected, settlement fees, subscription fees per plan, and total net revenue. Show a compliance dashboard: count of merchants in each KYC status, count of pending document reviews, and count of active disputes across the platform. Show a churn risk panel highlighting merchants with declining GMV month-over-month.

LAYER 6 — INVOICING & TAX

[SCREEN: Invoice Creator]
Build a full-featured invoice creation screen. Show a form with: invoice number (auto-generated with option to override), invoice date, due date, currency selector, client's GSTIN (auto-filled from profile), client's business name and address. Show a bill-to section: customer business name, customer GSTIN, customer address with pincode auto-fill. Show a line items table where each row has: item description, HSN code (with search lookup), quantity, unit of measure, unit price, discount (percentage or flat), taxable amount, GST slab selector (0%, 5%, 12%, 18%, 28%), CGST amount, SGST amount, IGST amount (auto-switched based on whether supply is inter-state or intra-state), and line total. Allow adding and removing rows. Below the table show: subtotal, total discount, total taxable value, CGST total, SGST total, IGST total, cess if applicable, invoice total in figures, and invoice total in words. Show a notes field and terms and conditions field. Show action buttons: save as draft, preview, send to customer (email or WhatsApp), download PDF, and mark as paid. Show a payment link attachment option: auto-generate a payment link for the invoice total and include it in the email.

[SCREEN: E-Invoice (IRP Integration)]
Build the e-invoice generation and management screen for merchants above the mandatory IRN threshold. Show an eligibility indicator showing the merchant's annual turnover and whether e-invoicing is mandatory for them. Show an IRP connection setup section: enter GSP (GST Suvidha Provider) credentials, test connection button, and connection status. Show an e-invoice generation queue: invoices that have been created but do not yet have an IRN. Allow bulk IRN generation for all pending invoices with a single button. Show the e-invoice detail view: the original invoice data, the generated IRN (Invoice Reference Number), the signed QR code payload, and the acknowledgement number and date from NIC. Show a reprint option that adds the IRN and QR code to the invoice PDF. Show an e-invoice cancellation flow with cancellation reason selector and confirmation, noting the 24-hour cancellation window restriction.

[SCREEN: GST Tax Report & GSTR-1 Export]
Build the GST reporting screen. Show a tax period selector (monthly or quarterly). Show a summary of tax liability for the selected period: total taxable value of outward supplies, total CGST, total SGST, total IGST, total cess, and grand total GST liability. Show a breakdown by GST slab: number of invoices and taxable value at each rate (0%, 5%, 12%, 18%, 28%). Show a B2B supplies section (invoices raised to GSTIN-registered customers) and a B2C supplies section (invoices raised to unregistered customers). Show a credit notes summary with original invoice reference. Show an export section with: download GSTR-1 in JSON format for direct IRP upload, download GSTR-1 in Excel format for CA review, and download a detailed tax register as CSV with one row per invoice. Show a GSTR-2B reconciliation section where the merchant can upload their GSTR-2B data and the system matches input tax credit claims against their purchase invoices.

[SCREEN: Invoice Management — Bulk Operations]
Build the bulk invoice management screen. Show a bulk invoice creation section: upload a CSV template (with downloadable sample) containing columns for customer name, customer email, customer GSTIN, line item descriptions, amounts, HSN codes, and GST slabs. Show a validation preview after upload: row count, validation errors flagged with row number and error description, valid rows count. Allow the user to fix errors inline or download the error report. Show a generate button that creates all valid invoices in one job with a progress indicator. Show a bulk send section: after generation, allow sending all invoices to respective customer emails in one action. Show an invoice status list with per-invoice status: generated, sent, opened, paid, overdue, cancelled. Include a bulk export option for downloading all invoices for a date range as a ZIP of PDFs or a single merged PDF.

[SCREEN: Credit Notes & Refund Invoicing]
Build the credit note management screen. Show a create credit note form: select the original invoice to credit against, enter credit note number (auto-generated), credit note date, and reason (return of goods, service not rendered, discount adjustment, data entry correction). Show a line items section pre-populated from the original invoice with editable quantities and amounts for partial credits. Show auto-calculated reversed GST amounts. Show actions: save, preview, send to customer, and download PDF. Show the credit note linked to its original invoice in a thread view. Include a full or partial refund initiation button linked to the credit note, triggering a refund to the original payment method. Show a credit notes register: list of all credit notes with original invoice reference, credit note number, date, amount, GST reversed, and status (draft, issued, linked to refund).

LAYER 7 — DIFFERENTIATOR & INNOVATIVE FEATURES

[SCREEN: Dynamic Checkout Personalisation Engine]
Build the configuration and analytics screen for the dynamic checkout personalisation feature. Show a configuration section where the client enables personalisation and sets the signals to use: device type, operating system, browser, customer return history, geographic location (state/city), time of day, and transaction amount range. Show a method scoring logic section: configure which signals boost or suppress each payment method's priority score. For example: if device is iOS, boost Apple Pay score; if location is Tier-2 city, boost UPI; if amount is above ₹50,000, suppress wallets. Show a live personalisation log: for recent checkout sessions, show the signals captured, the method ranking generated, the method the customer selected, and whether the transaction succeeded. Show a conversion lift report: comparing conversion rate on sessions with personalisation enabled versus a control group, broken down by payment method, device type, and geography.

[SCREEN: Payment Health Score Dashboard]
Build the payment health score screen shown to each merchant. Show a large circular or gauge visualisation of the merchant's current health score (0–100) with colour coding: 80–100 green (excellent), 60–79 amber (needs attention), 0–59 red (critical). Show the score components as a breakdown: conversion rate contribution, failure rate contribution, dispute rate contribution, refund rate contribution, and settlement delay contribution, each with sub-score and weight. Show a peer benchmark panel: how this merchant compares to the average score of merchants in the same business category. Show a score history chart: health score trend over the last 90 days. Show an action recommendations panel: if any component is dragging the score down, show the specific issue, root cause analysis, and recommended action with a direct link to the relevant settings screen. Show a "Share health report" button for the merchant to export a PDF health report for internal use.

[SCREEN: AI Cash Flow Forecasting]
Build the cash flow forecasting screen. Show a forecast chart with: historical daily collections for the last 90 days as a solid line, and projected daily collections for the next 30, 60, and 90 days as a dashed line with a confidence band (upper and lower bounds). Show forecast inputs the merchant can adjust: expected new customer growth rate, upcoming seasonal events (selectable: Diwali, New Year, end of financial year, sale event), and any manual one-time amount to add to the forecast (e.g. a known large invoice due). Show a summary forecast card for each period: expected collections next 30 days, expected collections next 60 days, expected collections next 90 days, with low and high range. Show a cash flow health indicator: whether expected collections are sufficient to cover upcoming auto-pay mandate collections and invoice payments. Show a methodology transparency note explaining that the forecast is based on the merchant's historical payment patterns and mandate schedule.

[SCREEN: Intelligent Dispute Autopilot]
Build the AI-assisted chargeback and dispute management screen. Show a disputes queue with columns: transaction ID, dispute ID, disputed amount, dispute reason code, issuing bank, date filed, response deadline (with days remaining countdown), status, and autopilot status (evidence auto-assembled, needs manual review, submitted, won, lost). Show a dispute detail view where the autopilot has assembled the evidence packet. Show each evidence item collected automatically: transaction log with timestamps, device fingerprint at time of transaction, IP address and geographic match to customer on file, 3DS authentication confirmation, delivery confirmation (if connected to logistics API), email receipt delivery confirmation, and prior successful payments from same customer. Show a review and edit interface where the merchant can add additional evidence before submission. Show the one-click "Submit to bank" action with a confirmation modal. Show a dispute analytics panel: win rate overall, win rate by dispute reason code, average resolution time, and total amount recovered vs written off.

[SCREEN: Payment DNA — Frictionless Return Checkout]
Build the Payment DNA management screen for returning customers. Show a configuration section explaining the feature: a privacy-safe behavioural fingerprint that allows returning customers to complete checkout with minimal re-entry, without storing raw card or bank data. Show consent management: how customer consent is captured at first checkout, opt-out link mechanics, and data retention duration. Show a usage analytics panel: number of customers with a DNA profile, percentage of return checkouts that used DNA-assisted checkout vs full re-entry, average time to complete using DNA vs manual, and conversion rate improvement. Show a data governance section: allow the merchant to see aggregate DNA profile counts (not individual data), and a bulk delete option to purge all DNA profiles for their customer base. Show a customer support lookup: enter a customer's masked identifier to check if they have a DNA profile and provide a one-click deletion option for support teams.

[SCREEN: Embedded Finance SDK — Integration Hub]
Build the developer integration hub for the embeddable SDK feature. Show a code snippet generator: select platform (React, Vue, Flutter, React Native, iOS Swift, Android Kotlin), select payment methods to embed, and generate a ready-to-paste code snippet with the correct API key placeholder and configuration object. Show a live preview panel rendering the embedded checkout component as it would appear in the client's app, based on their white-label configuration. Show an integration health panel for merchants who have deployed the SDK: SDK version in use, whether the latest version is installed, number of active embedded checkout instances, error rate from SDK deployments, and a changelog of recent SDK updates. Show a webhook event log scoped to SDK-initiated transactions. Include a migration guide for merchants moving from the hosted checkout to the embedded SDK.

[SCREEN: Collaborative Payment Links — Group Pay]
Build the group payment link creation and management screen. Show a create group link form: title, total target amount, currency, payment methods accepted, deadline for all contributions, and optional per-person contribution amount (fixed or open). Show a link sharing panel: a generated shareable link and QR code for the group link. Show a contribution tracker: a visual progress bar showing amount collected vs target, a list of contributors (masked identifiers, amount contributed, time contributed, method used), and number of pending contributors. Show a real-time notification configuration: alert the link creator when 50%, 75%, and 100% of the target is reached. Show a group link completion workflow: when the target is reached, auto-generate a combined receipt showing all contributors, and trigger settlement to the creator's account. Show a reminder action: send a reminder to all contributors who have not yet paid, customisable message, sent via the merchant's configured channels.

[SCREEN: Regulatory Radar]
Build the regulatory monitoring and alerts screen. Show a live feed of regulatory updates relevant to the merchant's active payment methods and compliance mode. Each update card shows: source (RBI, NPCI, SEBI, MCA, PCI DSS, EU PSD2, etc.), date published, headline summary, impact assessment (critical, moderate, informational), which of the merchant's active features or integrations are affected, recommended action, and action deadline if applicable. Show an alert configuration section: toggle which regulatory bodies and categories to monitor, and set notification preferences (email, dashboard bell, webhook). Show a compliance calendar: upcoming compliance deadlines on a timeline (e.g. UPI AutoPay mandate notification window changes, TPAP compliance submissions, PCI DSS renewal). Show a compliance documentation vault where merchants upload and store their own compliance certificates with expiry reminders (PCI SAQ, ISO certifications, RBI PA licence if applicable).

[SCREEN: Adaptive MDR Engine — Fee Configuration]
Build the merchant discount rate configuration screen for super admins. Show a base fee structure table: for each payment method show the standard MDR, processing fee, and any fixed per-transaction fee. Below show an adaptive rules builder: create rules that override the base fee for a merchant or merchant segment based on: monthly GMV tier (e.g. above ₹50 lakhs, above ₹1 crore, above ₹10 crore), payment method mix (e.g. if more than 60% of volume is UPI, reduce card MDR), risk score (reduce fee for low-risk merchants), and business category. Show a fee simulation tool: input a hypothetical transaction count, average order value, and method mix, and see the projected fees under the current rule set. Show a fee change history log for audit compliance. Show a merchant-facing fee breakdown screen (separate from super admin view) where clients can see their current applicable MDR per method, any volume discounts they have unlocked, and the GMV required to reach the next tier.

[SCREEN: Carbon Offset Micro-Checkout Feature]
Build the carbon offset feature configuration and reporting screen. Show a feature enablement toggle for the merchant with a configuration section: offset amount options to offer to customers (₹1, ₹2, ₹5, or a custom amount), display placement (below payment button, as a pre-checkout opt-in, or as a post-payment thank you page option), and messaging customisation (e.g. "Add ₹2 to offset the carbon footprint of this order"). Show a consent and transparency section: link to the verified carbon credit programme the offset funds are directed to, programme certification details, and an explanation of how the offset amount is calculated. Show a merchant ESG impact dashboard: total carbon offset credits purchased through the merchant's checkouts, number of contributing customers, equivalent real-world impact metric (e.g. trees planted equivalent, CO₂ kg offset), a monthly trend chart, and a downloadable ESG impact report PDF. Show an embeddable impact badge generator: a dynamic badge the merchant can place on their website showing cumulative offset impact, with an embed code.

[SCREEN: Voice-Triggered Payment Request — Integration]
Build the voice integration setup and management screen. Show supported voice platform connections: Amazon Alexa skill setup (with client ID and secret), Google Assistant action setup, and a web-based voice shortcut for desktop users. Show a voice command templates section: pre-built command phrases the merchant can activate, e.g. "Send invoice to [contact name] for [amount]", "Create payment link for [amount] for [description]", "Check payment status for [transaction ID]". Show a contacts directory section: a list of saved customer contacts with names and phone numbers or WhatsApp numbers, so that voice commands can match "Raj" to the correct customer profile. Show a voice command log: all voice-triggered actions with command text captured, action taken, payment link generated, and whether payment was completed. Show error handling: when a contact name is ambiguous or amount is not recognised, show how the system requests clarification and falls back gracefully.

[SCREEN: Smart Split Settlement — Marketplace Configuration]
Build the split settlement configuration screen for marketplace clients. Show a vendor or sub-merchant management section: add sub-merchant accounts with their bank account details, verified status, and settlement percentage or fixed split. Show a split rule builder: for each product category or payment method, define the split configuration — platform percentage, vendor percentage, logistics partner percentage (if applicable), and any fixed fee deducted before split. Show a transaction-level split preview: for a sample order amount, display how the split would be applied and the net amount each party receives after fees. Show a multi-party settlement log: each settlement batch showing the amounts disbursed to each sub-merchant with individual UTR numbers. Include a sub-merchant dashboard access section: configure whether vendors receive read-only access to their own settlement and transaction data through a restricted portal.

[SCREEN: B2B Payment Terms Engine]
Build the net payment terms management screen for enterprise and B2B clients. Show a terms configuration section: define available payment terms offered to buyers (Net-15, Net-30, Net-45, Net-60, custom days). Show a buyer onboarding section: add business customers with their GSTIN, credit limit, and assigned payment terms. Show an open invoices ledger: each outstanding B2B invoice with buyer name, invoice number, invoice date, due date, amount, days remaining or overdue, and status (not due, due soon, overdue, in dunning, paid). Show an automated collection configuration: on the due date, the system initiates collection via the buyer's registered payment method (NACH, ACH, SEPA, or UPI AutoPay mandate). Show a credit risk panel: flag buyers approaching their credit limit or with overdue balances exceeding a configured threshold. Show a buyer payment history view: for each buyer, a track record of invoices, payment dates, and average days to pay.

LAYER 8 — GLOBAL CROSS-CUTTING SCREENS

[SCREEN: Notifications & Alert Centre]
Build a centralised notifications and alerts screen. Show a notification feed with categories: transaction alerts (large transactions, failed transactions above threshold), settlement alerts (settlement credited, settlement delayed), dispute alerts (new dispute filed, response deadline approaching, dispute won or lost), compliance alerts (document expiring, KYC action required, regulatory update), system alerts (payment method downtime, API error spike, webhook delivery failure), and reminder delivery reports (SMS delivered, email bounced, WhatsApp undelivered). Show a notification preferences panel: for each category, allow the merchant to set delivery channel (in-app, email, SMS, WhatsApp), frequency (instant, hourly digest, daily digest), and minimum threshold (e.g. only alert for transactions above ₹10,000). Show an alert history with filter by category, date range, and read or unread status. Show a bell icon in the top navigation with an unread count badge that links to this screen.

[SCREEN: Refund Management]
Build the refund management screen. Show a refund initiation form: search for a transaction by ID, customer phone, or order reference. Show transaction details on selection: original amount, method used, date, and current refund eligibility (full refund, partial refund, refund window expired). Show a refund amount input (for partial refunds), refund reason selector (customer request, duplicate payment, order cancelled, product not delivered, pricing error, other), and internal notes field. Show a confirmation step with final refund amount and destination (original payment method, bank transfer, wallet credit). Show a refunds log with columns: original transaction ID, refund ID, refund amount, refund reason, initiated by (staff name), initiated date, status (processing, credited, failed), and completion date. Show refund analytics: total refund amount this month, refund rate percentage, refund rate by method, and top refund reasons as a pie chart.

[SCREEN: Customer Support Lookup Tool]
Build a customer-facing transaction lookup tool for merchant support teams. Show a search bar accepting: transaction ID, payment link reference, customer phone number (masked search), customer email (masked search), or order ID from the merchant's system. On search, display the transaction record with: transaction ID, order ID, amount, method, gateway reference, status, timestamps for each status change (initiated, processing, authorised, captured, settled), customer device info (device type, OS, browser), failure reason if failed, refund history if any, and dispute history if any. Show a "Copy full transaction details" button to assist in escalation. Show an action panel on the right: initiate refund, raise dispute, resend payment receipt to customer, and flag transaction for risk review. Ensure all customer PII (name, email, phone) is masked by default with a permission-gated unmask option requiring a reason to be logged.

[SCREEN: Mobile Responsive Checkout — Adaptive Layout]
Build the mobile-specific layout of the checkout page. The mobile checkout must show: a collapsible order summary at the top with a tap-to-expand interaction, a payment method selector using large touch-friendly tap targets instead of small radio buttons, a numeric keypad for card number and OTP inputs, an auto-advance mechanism moving focus to the next field after the correct number of characters are entered (16 digits for card number, 4 digits for expiry month, 4 digits for expiry year, 3 digits for CVV), a UPI intent section showing large icons for the top 4 UPI apps installed on the device, a full-screen QR display mode for UPI QR with a brightness-boost suggestion, a bottom sheet interaction pattern for payment method selection on small screens, and a persistent sticky footer showing the total amount and the primary CTA button at all times. Show loading and processing states with appropriate feedback to prevent duplicate submission taps.